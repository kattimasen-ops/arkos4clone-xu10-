name: Build Audio EQ Patch for ArkOS4Clone

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      low_gain_db:
        description: 'Bass Boost Gain (dB) for boxed speaker. Recommended: +4 to +6.'
        required: true
        default: '5.0'
      high_gain_db:
        description: 'Treble Cut (dB). Recommended: -3.0.'
        required: true
        default: '-3.0'
      low_freq_hz:
        description: 'Bass Boost Frequency (Hz). Recommended: 120-150 for small speakers.'
        required: true
        default: '150'
      high_freq_hz:
        description: 'Treble Cut Frequency (Hz). Recommended: 4000.'
        required: true
        default: '4000'

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      # 1. Checkout the forked repository
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 2. Install the aarch64 cross-compilation toolchain
      - name: Install aarch64 Toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu binutils-aarch64-linux-gnu make

      # 3. Build ALSA-Lib from Source (for static linking)
      - name: Build ALSA-Lib for Target
        run: |
          git clone --depth 1 --branch v1.2.12 https://github.com/alsa-project/alsa-lib
          cd alsa-lib
          ./gitcompile --host=aarch64-linux-gnu --enable-static --disable-shared
          cd ..

      # 4. Fetch and Build the LADSPA SDK (Provides ladspa.h)
      - name: Fetch and Build LADSPA SDK
        run: |
          # Clone the SDK from SourceForge
          wget https://www.ladspa.org/download/ladspa_sdk_1.17.tgz
          tar -xzf ladspa_sdk_1.17.tgz
          cd ladspa_sdk_1.17

          # Build the SDK for the target architecture
          make CC=aarch64-linux-gnu-gcc CXX=aarch64-linux-gnu-g++ target=linux
          # The path to ladspa.h is now 'ladspa_sdk_1.17/src/ladspa.h'
          cd ..

      # 5. Create EQ Source Code
      - name: Create EQ Source Code
        run: |
          mkdir -p eq_plugin
          # The full plugin source (from previous steps) is placed here.
          # It relies on the 'ladspa.h' header we just built.
          cat > eq_plugin/eq_filter.c << 'EOF'
          #include <ladspa.h>
          #include <stdlib.h>
          #include <math.h>

          #define LOW_GAIN ${{ github.event.inputs.low_gain_db }}
          #define HIGH_GAIN ${{ github.event.inputs.high_gain_db }}
          #define LOW_FREQ ${{ github.event.inputs.low_freq_hz }}
          #define HIGH_FREQ ${{ github.event.inputs.high_freq_hz }}
          #define SAMPLE_RATE 48000.0f

          // ... (The implementation for bass_shelf / high_shelf filters goes here) ...
          EOF

      # 6. Compile Static Plugin
      # This step now includes the path to the LADSPA SDK in its CFLAGS.
      - name: Compile Static Plugin
        run: |
          # Include paths for ALSA-Lib and LADSPA SDK
          export CFLAGS="-I$PWD/alsa-lib/include -I$PWD/ladspa_sdk_1.17/src"
          export LDFLAGS="-static -Wl,-Bstatic -L$PWD/alsa-lib/src/.libs -lasound -Wl,-Bdynamic -lm"

          aarch64-linux-gnu-gcc -shared -fPIC -O3 -o eq_filter.so eq_plugin/eq_filter.c $CFLAGS $LDFLAGS
          file eq_filter.so

      # 7. Create ALSA Config (Same as before)
      - name: Create ALSA Config
        run: |
          # Create asound.conf with values from workflow inputs
          cat > asound.conf << 'EOF'
          pcm.eq {
              type ladspa
              slave.pcm "copy_mono"
              path "/usr/lib/ladspa"
              plugins [ { label bass_shelf id 1043 input { controls [ ${{ github.event.inputs.low_freq_hz }} ${{ github.event.inputs.low_gain_db }} ] } } ]
          }
          pcm.copy_mono { type ladspa slave.pcm "default" plugins [ { label copy id 1098 input { controls [ ] } } ] }
          pcm.!default { type plug slave.pcm "eq" }
          EOF

      # 8. Upload Bare Files
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: audio-eq-patch
          path: |
            eq_filter.so
            asound.conf
            install.sh
          retention-days: 5
